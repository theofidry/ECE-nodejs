extend layout

block content
    h1.page-header
        | Login to access to secure session.
    form.form-signin(role='form', action='/', method='post')
        h2.form-signin-heading
            | Please sign in
        if errors !== undefined
          if errors.msg !== undefined
            p #{errors.msg}
        label.sr-only(for='username') Username:#{' '}
        //TODO: change the pattern of the username contrôle to allow points or -, ex to do "theo.fidry" or "theo-fidry"
        //ex to allow "theo.fidry"...
        input#username.form-control(type='text', name='username', pattern='[A-Za-z]{1,32}', placeholder='Username',
        required)
        label.sr-only(for'passowrd') Password:#{' '}
        input#password.form-control(type='password', name='password', pattern='.{4,32}',
        placeholder='password', required)
        button.btb.btn-lg.btn-primary.btn-block(type='submit') Sign in
    
      
